/*
 * FigureInterface which has an area and a perimeter
 */
public interface FigureInterface {
     /*
     * Each figure has an area
     */
    double area();
     /*
     * Each figure has a perimeter
     */
    double perimeter();
}